# Dependabot configuration
#
# `deployment/gcp` is a Terraform HCL workspace (migrated from cdktf). Dependabot
# tracks the Terraform provider version constraints declared there.
version: 2
updates:
  - package-ecosystem: "terraform"
    directory: "/deployment/gcp"
    schedule:
      interval: "weekly"
